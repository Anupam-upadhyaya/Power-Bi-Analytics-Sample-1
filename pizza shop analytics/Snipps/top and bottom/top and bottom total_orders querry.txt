select*from pizza_sales

select top 5 pizza_name, cast(count(distinct order_id) as decimal(10,2)) as total_orders from pizza_sales
group by pizza_name
order by total_orders desc

select top 5 pizza_name, cast(count(distinct order_id) as decimal(10,2)) as total_orders from pizza_sales
group by pizza_name
order by total_orders asc-- even if we do not write asc default table will be in ascending order  